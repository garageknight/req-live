const services = require('../services');
const app = require('../server')(services);

const port = services.config.has('Web.port') ? services.config.get('Web.port') : 8000;

app.listen(port, () => {
  services.logger.info('Web Server running at localhost: ' + port);
});
